[package]
name = "agnt5-sdk-core"
version = "0.1.0"
edition = "2021"
authors = ["AGNT5 Team"]
description = "High-performance Rust core for AGNT5 SDK"

[lib]
name = "agnt5_sdk_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
# gRPC and Protocol Buffers
tonic = "0.12"
prost = "0.13"
prost-types = "0.13"

# Async runtime
tokio = { version = "1.0", features = ["full"] }
tokio-stream = "0.1"
async-stream = "0.3"
flume = "0.11"

# Error handling
anyhow = "1.0"
thiserror = "1.0"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# UUID generation
uuid = { version = "1.0", features = ["v4"] }

# Random number generation for jitter
rand = "0.8"

# Additional logging and time dependencies
lazy_static = "1.4"
chrono = { version = "0.4", features = ["serde"] }

# Build dependencies
[build-dependencies]
tonic-build = "0.12"

# Examples
[[example]]
name = "debug_registration"
path = "examples/debug_registration.rs"